# Database
DATABASE_URL="postgresql://padel_user:padel_password@localhost:5432/padel_stats_db"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Server
PORT=3001
NODE_ENV=development

# CORS - This will be handled dynamically in production
CORS_ORIGIN="http://localhost:3000"

# Production overrides (these will be set by Render)
# DATABASE_URL will be provided by Render's PostgreSQL addon
# PORT will be provided by Render
# NODE_ENV will be set to 'production' by Render 